Upstream-Status: Pending

Fix -Werror=format-security by passing strings as format arguments instead
of raw buffers. Aligns with hardened builds used in our distro.

--- a/src/cdda-player.c	2025-12-18 19:11:25.308973925 +0100
+++ b/src/cdda-player.c	2025-12-18 19:11:25.408973198 +0100
@@ -292,7 +292,7 @@
              psz_action);
   else
     snprintf(psz_action_line, sizeof(psz_action_line), "%s", "" );
-  mvprintw(LINE_ACTION, 0, psz_action_line);
+  mvprintw(LINE_ACTION, 0, "%s", psz_action_line);
   clrtoeol();
   refresh();
 }
@@ -905,7 +905,7 @@
     display_line(LINE_YEAR,   0, "CD Year         : %s", year);
   }
   
-  mvprintw(LINE_CDINFO, 0, (char *) "CD info: %0s", line);
+  mvprintw(LINE_CDINFO, 0, (char *) "CD info: %s", line);
   clrtoeol();
   action(NULL);
 }
@@ -1018,10 +1018,10 @@
       }
       if (sub.track == i) {
         attron(A_STANDOUT);
-        mvprintw(i_line++, 0, line);
+        mvprintw(i_line++, 0, "%s", line);
         attroff(A_STANDOUT);
       } else 
-        mvprintw(i_line++, 0, line);
+        mvprintw(i_line++, 0, "%s", line);
       clrtoeol();
     }
   }
